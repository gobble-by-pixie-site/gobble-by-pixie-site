# Gobble by Pixie â€” storefront

- Astro static site, **vanilla CSS design tokens** (`src/styles/global.css`) â€” deliberately no Tailwind, no component library.
- **Design pivoted 2026-07-30** to an editorial style matching client-approved reference `miampatisserie.com`: muted dusty-rose/cream palette, sharp corners (not rounded pills), unified Cormorant Garamond/EB Garamond typography (dropped the earlier Playfair Display + colorful multi-hue direction). If you see references to a "colorful boutique" palette elsewhere (old commits, PLAN.md history), that's superseded â€” current tokens are the source of truth.
- Real brand assets in use: logo (`public/Logos/Gobble Logo-TM-Black.png` â€” black variant chosen for contrast against the light/cream site; red/white variants also present but unused), real product photography (`public/Cover photo.JPG`, `public/Green Jar.png`).
- **Console (ARM Postgres on `immich-arm`) is the single source of truth** â€” no direct DB access from this repo. Menu/products/collections/site copy/analytics IDs all come from Linear Console's `/api/public/*` at request time with a 60s in-memory cache. Console edits are live in â‰¤60s â€” no rebuilds. Site content (about page eyebrow/heading/values, home page hero/sub/trust, BYOP copy, best-sellers copy) is managed via Console â†’ Settings â†’ Content and backfilled as of 2026-08-24. The old Google Sheets CSV fallbacks remain in `lib/*.js` as dead-code paths only.
- Site copy (hero eyebrow/sub/trust text, footer tagline, fulfillment banner, FSSAI/GSTIN, grazing-table price, about-page intro) is wired to the SiteContent tab (`src/lib/fetchSiteContent.js`, `GOOGLE_SITE_CONTENT_CSV_URL` env var) as of 2026-07-31 â€” she can edit the `value` column and rebuild+redeploy to update text without touching code. Falls back to hardcoded defaults per-key if the Sheet value is blank or the CSV fetch fails, so a half-filled Sheet can't break the site. **Use the standard gid-based `export?format=csv&gid=<id>` URL, same pattern as `GOOGLE_SHEET_CSV_URL`** â€” a `gviz/tq?tqx=out:csv&sheet=<name>` URL was tried first and looked more robust on paper (survives tab reordering) but Google's gviz export reproducibly squashed most rows of this exact sheet into one row while the gid-based export read it perfectly; root cause never identified, just avoid gviz here. Not yet wired: the H1 "Handmade Flavoured Cream Cheese" wordmark (kept hardcoded â€” it's split into two `<span>`s for the gradient effect, not a plain string) and the FAQ page's dispatch cards (structured differently from the banner's combined string, left as-is).
- Account/rewards system (`/account`, `/login`, `/signup`) uses localStorage session (`gbp_auth`, `gbp_customer`) + a Google Apps Script backend (`APPS_SCRIPT_URL` env var). **This backend is built, deployed, and verified working end-to-end in production** â€” `code.gs` in this repo is the deployed script (also mirrored in the client's Sheet's own Script Editor; keep both in sync manually, there's no CI push to Apps Script). Covers: signup/signin/password-reset, profile updates, points/tiers (Nibbler â†’ Foodie â†’ Gobbler VIP), get-orders, coupon validation, GST reverse-calculation on orders, and Razorpay webhook handling (order creation/refund/cancellation) â€” the Razorpay pieces are fully built but return a graceful "not set up yet" error until real API keys are added to `code.gs`'s `CONFIG`.
- **Cart**: `public/cart.js` is a plain-JS, localStorage-backed shopping cart shared across every page (loaded globally via a `<script>` tag in `BaseLayout.astro`, exposed as `window.GobbleCart`). Every "add to order" action on the site (homepage cards, menu Quick View modal, BYOP custom platters, account-page Reorder) adds to this cart, not a per-item WhatsApp link. Checkout currently builds one combined WhatsApp message from the cart contents â€” swap that step for real payment once Razorpay keys land. Cart items now carry a GST breakdown (Taxable Value / CGST 2.5% / SGST 2.5% / Total), computed client-side in `cart.js`'s `gstBreakdown()` mirroring `code.gs`'s `computeGstBreakdown()` exactly â€” same intra-state (Delhi NCR) assumption caveat applies. **Important Astro gotcha**: `.cart-item*`, `.cart-empty`, `.qty-btn` styles live in `global.css`, not in `BaseLayout.astro`'s scoped `<style>` block â€” those elements are built via `innerHTML` in `renderCart()` at runtime, so they never receive Astro's scoped `data-astro-cid` attribute and a scoped rule would silently never match. Any future cart-item markup change needs its CSS added to `global.css`, not the layout's own style block.
- **Events page** (`/events`, built 2026-07-31): reads the Events tab (`src/lib/fetchEvents.js`, `GOOGLE_EVENTS_CSV_URL` env var) â€” photo + title + date + description per event, only rows with `published=TRUE` show. Reuses `convertImageUrl()` exported from `fetchProducts.js` for Drive-link handling, same convention as Products. Header nav's Menu dropdown is now built dynamically from live Products categories (`BaseLayout.astro` calls `fetchProducts()`/`getCategories()` itself) instead of a hardcoded list â€” same pattern `theclosetstory.com/tcs-store`'s Categories dropdown uses â€” so adding a category row to the Sheet makes it appear in the dropdown and menu pages on the next rebuild, no code change needed.
- **Menu restructured into per-category pages** (2026-07-31, replacing the old single-page-with-anchor-sections design): `/menu` is now a hub of category tiles, `/menu/[category].astro` is a `getStaticPaths()`-driven dynamic route generating one real page per live Products category â€” exact pattern copied from `theclosetstory.com/tcs-store/src/pages/category/[category].astro`. `/menu/grazing-tables` is a static sibling page (Grazing Tables isn't a real Products category, just a service enquiry). Every category page has a sticky sub-nav linking to every *other* category page (not anchors), so browsing across categories doesn't require going back through the hub. `ProductQuickViewModal.astro` is a shared component included on each category page rather than duplicated. `slugify()` (in `fetchProducts.js`) is the single source of truth for category-name â†’ URL-slug, used by both `BaseLayout.astro`'s dropdown and every `/menu/*` page's `getStaticPaths`/links â€” never hardcode a slug.
- **Favicon + social share image** (2026-07-31): the old `<link rel="icon" href="/favicon.svg">` pointed at a file that didn't exist â€” the site had no working favicon at all. `public/favicon-32.png`, `favicon-16.png`, `apple-touch-icon.png` are generated (via `sharp`, the same image lib Astro already depends on) by cropping just the "g" mark out of the wordmark logo and compositing it onto a parchment-colored square â€” the raw logo files in `public/Logos/` are wide wordmarks, unreadable at favicon size. Full OG/Twitter meta tags added too (previously none existed); `public/og-image.jpg` is the full wordmark centered on the same parchment background, 1200Ã—630.
- **Cart vs. WhatsApp-enquiry split** (2026-07-31): "price on request" products never enter the cart â€” the cart's GST math assumes every line has a real price. `ProductQuickViewModal.astro` and `ProductCard.astro`'s no-modal fallback both check `hasPrice` and, if false, open a direct WhatsApp enquiry for that product instead of adding anything. Button reads "Add to Cart" for priced items, "Enquire on WhatsApp" for unpriced ones (was "Add to Order" for everything, including unpriced items that silently entered the cart with a null price â€” a real bug, not a naming nitpick).
- Full build plan, scope decisions, and BOS integration roadmap: `../PLAN.md` (one level up, shared with the non-website phases â€” this is a planning doc, not deployed with the site).
- **Deploy target**: Cloudflare Pages, project `gobble-by-pixie-site`, under the client's own `gobblebypixie@gmail.com` account (not a Linear Ventures account). As of 2026-07-31 the project is **Git-connected** (GitHub `gobble-by-pixie-site/gobble-by-pixie-site`, branch `main`) â€” any push to `main` triggers a real production rebuild automatically. A manual `wrangler pages deploy dist` (OAuth login, not an API token â€” token auth hit a reproducible wrangler bug on this project, never root-caused) still works as a fallback but isn't the primary path anymore; don't push design/experimental branches to `main` casually, it's live now.
- **Auto-deploy on Sheet edit** (built 2026-07-31): a Cloudflare Deploy Hook is wired into `code.gs` (`onProductSheetChange` + `flushPendingDeploy`, installed via `installAutoDeployTriggers()`). Editing Products, SiteContent, Events, or Coupons sets a debounce timestamp instead of firing immediately; a 1-minute time-driven trigger flushes it once edits go quiet for 90s, so a burst of edits (e.g. several photo uploads) triggers one rebuild, not several. Orders/Customers/Webhook Errors edits are explicitly skipped. `DEPLOY_HOOK_URL` in `code.gs` is a real, live secret-ish URL (anyone with it can trigger a build, though not read/write data) â€” don't paste it into public places.
- **Live domain**: https://gobblebypixie.com
- **SEO (fixed 2026-08-30):** custom `src/pages/sitemap.xml.js` — curated static pages + product URLs pulled LIVE from Console (new jars appear without a deploy); @astrojs/sitemap removed (its build output missed every product and listed account/login/preview-sections). `public/robots.txt` carries the `Sitemap:` line (CF's managed content-signals block prepends to it). Organization JSON-LD sitewide in BaseLayout (new `head` slot), Product JSON-LD on `/products/[slug]` (price/availability/image from Console data), `preview-sections` now actually noindex. Verified live: 45 URLs incl. all 37 products. (cutover from Squarespace completed 2026-07-30/31 â€” DNS migrated to Cloudflare, SSL active, MX/email records preserved).

See `README.md` for local dev setup and full file map.

## 2026-09-01 - Cross-tenant leak fix (CRITICAL)

- **Console API env (CRITICAL):** this site's CF Pages dashboard MUST use its
  OWN `CONSOLE_STOREFRONT_API_KEY`:
  `396d9b1deb929b7a635afd26ae21866e62b64e19f48ad8a756f4168df7a2b658` and
  `CONSOLE_API_URL=https://console.linearventures.in`.
- **What happened:** on 2026-09-01 the dashboard had TCS's key, so this site
  rendered TCS products/hero copy/images. The console scopes by key, so a
  wrong key returns the wrong tenant's data (RLS was NOT the cause).
- **Verify after any env edit:** fetch the live site and confirm Gobble
  products/images, not TCS.
- **Revalidation secret:** set `REVALIDATE_SECRET` =
  `f62750bb322621c712be35724324bcfb479e96b5c7bffa7905f917a3e8242bc8` as an
  encrypted secret (matches the console fallback) for instant purge.
- **Edge cache (2026-09-01):** middleware sets `s-maxage=60,
  stale-while-revalidate=30`. The old "Cache HTML 1hr" Cache Rule was deleted
  from this zone - do NOT re-create it (it made console edits take 1h to
  show). Console edits now appear within ~1 minute.